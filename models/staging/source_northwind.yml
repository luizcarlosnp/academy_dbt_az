sources:
  - name: northwind
    database: dbt_az
    schema: raw_northwind
    tables:
      - name: products
        description: Tabela bruta de produtos
        columns:
          - name: id
            data_tests:
              - not_null
              - unique
      - name: category
        description: Tabela bruta de categorias
        columns:
          - name: id
            data_tests:
              - not_null
              - unique